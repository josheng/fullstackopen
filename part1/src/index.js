import React from 'react'
import ReactDOM from 'react-dom/client'

import App from './App'

// this calls the component to render the content
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
